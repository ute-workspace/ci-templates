name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    uses: ute-workspace/ci-templates/.github/workflows/node-ci.yml@v1
    with:
      node-version: "20"
      working-directory: src
