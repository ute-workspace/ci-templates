# Project Discovery

Stage summary. The procedure is the `project-discovery` skill.

## Purpose

Build or refresh the baseline understanding of a project — stack,
structure, build/test/deploy tooling, CI/CD model and owner — and write it
into `docs/`. Entry point before planning or implementing on an unfamiliar
project.
