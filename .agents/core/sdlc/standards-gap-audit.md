# Standards Gap Audit

Stage summary. The procedure is the `standards-gap-audit` skill.

## Purpose

Audit an agent run against `core/`, `skills/`, and `core/archetypes/`
themselves — not a feature spec (`change-audit`) or a release
(`post-release-review`) — and classify each gap by where its fix belongs.
